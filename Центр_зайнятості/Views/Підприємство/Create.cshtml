@model Центр_зайнятості.Models.Підприємство

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Підприємство</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Назва" class="control-label"></label>
                <input asp-for="Назва" class="form-control" />
                <span asp-validation-for="Назва" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="РозташуванняОфісу" class="control-label"></label>
                <input asp-for="РозташуванняОфісу" class="form-control" />
                <span asp-validation-for="РозташуванняОфісу" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ПредставникПіб" class="control-label"></label>
                <input asp-for="ПредставникПіб" class="form-control" />
                <span asp-validation-for="ПредставникПіб" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
