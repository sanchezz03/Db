@model Центр_зайнятості.Models.ПерелікВакансій

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>ПерелікВакансій</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="КодПідприємства" class="control-label"></label>
                <select asp-for="КодПідприємства" class ="form-control" asp-items="ViewBag.КодПідприємства"></select>
            </div>
            <div class="form-group">
                <label asp-for="КодВакансії" class="control-label"></label>
                <select asp-for="КодВакансії" class ="form-control" asp-items="ViewBag.КодВакансії"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
