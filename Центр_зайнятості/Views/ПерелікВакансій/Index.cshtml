@model IEnumerable<Центр_зайнятості.Models.ПерелікВакансій>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.КодВакансіїNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.КодПідприємстваNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.КодВакансіїNavigation.КодВакансії)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.КодПідприємстваNavigation.КодПідприємства)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.КодЗапису">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.КодЗапису">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.КодЗапису">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
