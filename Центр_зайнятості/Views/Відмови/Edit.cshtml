@model Центр_зайнятості.Models.Відмови

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Відмови</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="КодВідмови" />
            <div class="form-group">
                <label asp-for="КодКлієнта" class="control-label"></label>
                <select asp-for="КодКлієнта" class="form-control" asp-items="ViewBag.КодКлієнта"></select>
                <span asp-validation-for="КодКлієнта" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ДатаВідмови" class="control-label"></label>
                <input asp-for="ДатаВідмови" class="form-control" />
                <span asp-validation-for="ДатаВідмови" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
