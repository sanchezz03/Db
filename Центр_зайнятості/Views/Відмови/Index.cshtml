@model IEnumerable<Центр_зайнятості.Models.Відмови>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ДатаВідмови)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.КодКлієнтаNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ДатаВідмови)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.КодКлієнтаNavigation.КодКлієнта)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.КодВідмови">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.КодВідмови">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.КодВідмови">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
